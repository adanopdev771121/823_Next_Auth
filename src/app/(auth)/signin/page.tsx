import SignTemplate from "../_components/SignTemplate";

export default function SignInPage() {
  return (
    <SignTemplate
      title="Sign in"
      question="Don't have an account?"
      switchPage="Sign up"
    />
  );
}
